import NavBar from "../NavBar/NavBar";

const Header = () => {
   return (
      <header>
         <NavBar />
         <h2>What's your plan today?</h2>
      </header>
   );
};

export default Header;
